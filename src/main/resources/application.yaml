eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
server:
  port: 8761
spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: ms-discovery-server
